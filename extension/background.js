chrome.runtime.onInstalled.addListener(() => {
  console.log("YouTube Summary extension installed.");
});
/* Currently unused, but kept for future tasks
   (alarms, storage migration, context menus, etc.). */
